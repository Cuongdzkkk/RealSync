# AI Module — RealSync

> Python FastAPI + OpenAI — Phân loại & Sinh nội dung AI

## Sub-modules
- `classification/` — AI Classification Service
- `content-engine/` — Content AI Engine
- `models/` — ML models & configs
- `scripts/` — Utility scripts

## Quick Start
```bash
cd classification
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

## Conventions
See `../agent-skills/SKILL.md` → Section 7 (AI Rules)
See `../agent-skills/references/ai-guide.md`
